{
  "version": 2,
  "name": "prtfolio",
  "builds": [
    {
      "src": "portfolio/package.json",
      "use": "@now/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "portfolio/$1" }]
}
